{% extends 'base.html.twig' %}

{% block title %}Modifier l'analyse{% endblock %}

{% block body %}
    <main class="form" aria-label="Formulaire de modification d'analyse">
        {# Lien de retour à la liste des analyses avec aria-label explicite #}
        <a href="{{ path('app_analysis_list') }}" aria-label="Retour à la liste des analyses">◀ Retour à list</a>

        <h1>Modifier l'analyse</h1>

        {# Inclusion du formulaire de modification avec bouton personnalisé #}
        {{ include('analysis/_form.html.twig', {'button_label': 'Mise à jour'}) }}
    </main>
{% endblock %}
